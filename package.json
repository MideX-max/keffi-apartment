{
  "name": "keffi-apartment-suites-system",
  "version": "1.0.0",
  "description": "KEFFI APARTMENT SUITES Guest Management & Access Control System",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:all": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "server": "npm --prefix backend start",
    "build": "npm install && npm install --prefix frontend && npm install --prefix backend && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "lint": "npm --prefix frontend run lint"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
